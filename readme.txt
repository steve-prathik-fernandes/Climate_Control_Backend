create virtual environment and activate in project directory,
python -m venv venv

next step,

For windows users, 
.\venv\Scripts\activate

For mac users,
source venv/bin/activate


To install all the necessary modules/libraries please run,
pip install -r requirements.txt
